<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:local="clr-namespace:FTCollectorApp.Utils"
             x:Class="FTCollectorApp.View.SitesPage.PortPage">
    <ContentPage.Content>
        <AbsoluteLayout HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand">
            <StackLayout AbsoluteLayout.LayoutFlags="All" AbsoluteLayout.LayoutBounds="0,0,1,1">
                <ScrollView>

                    <StackLayout Padding="20">
                        <Label Text="Port" FontSize="Large" />
                        <Label Text="Rack" />
                        <local:Dropdown x:Name="pRackNum" SelectedIndexChanged="OnIndexChanged"/>
                        <Label Text="Chassis" />
                        <local:Dropdown x:Name="pChassis" SelectedIndexChanged="OnIndexChanged"/>
                        <Label Text="Blade" />
                        <local:Dropdown x:Name="pBlade" SelectedIndexChanged="OnIndexChanged"/>                        
                        <Label Text="Port Number" />
                        <local:Dropdown x:Name="pPortNumber" SelectedIndexChanged="OnIndexChanged"/>
                        <Label Text="Transmit / Receive" />
                        <local:Dropdown x:Name="pTxRx" SelectedIndexChanged="OnIndexChanged" />
                        <Label Text="Rack" />
                        <local:BorderEntry />
                        <Label Text="Port type" />
                        <local:Dropdown x:Name="pPortType" SelectedIndexChanged="OnIndexChanged" />

                        <Button Text="Save and Continue" Clicked="btnSaveContinue"/>
                        <Button Text="Done Port" Clicked="btnExit"/>
                    </StackLayout>

                </ScrollView>
            </StackLayout>
            <!--<StackLayout IsVisible="{Binding IsBusy}" Padding="12"
                 AbsoluteLayout.LayoutFlags="PositionProportional"
                 AbsoluteLayout.LayoutBounds="0.5,0.5,-1,-1">

                <ActivityIndicator IsRunning="{Binding IsBusy}" Color ="{StaticResource blueColor}"/>

                <Label Text="Loading..." HorizontalOptions="Center" TextColor="White"/>

            </StackLayout>-->
        </AbsoluteLayout>
    </ContentPage.Content>
</ContentPage>