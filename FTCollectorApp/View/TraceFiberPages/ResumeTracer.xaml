<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:local="clr-namespace:FTCollectorApp.Utils"
             x:Class="FTCollectorApp.View.TraceFiberPages.ResumeTracer">
    <ContentPage.Content>
        <StackLayout>

            <Label Padding="0,20,0,20" Text="Resume Tracer" FontSize="Large" />
            <Label Padding="0,20,0,20" Text="TagNumber" />
            <local:Dropdown x:Name="pTagNumber" ItemsSource="{Binding BeginningSiteList }" 
                            ItemDisplayBinding="{Binding HosTagNumber}" SelectedItem="{Binding SelectedTagNum}"/>
            <Label Padding="0,20,0,20" Text="Duct" />
            <local:BorderEntry Text="" />
            <Label Padding="0,20,0,20" Text="Fiber Cable" />
            <local:BorderEntry Text="" />
 
            <Button Padding="10" Text="Resume Tracing" Command="{Binding ResumeCommand}"/>
            <Button Padding="10" Text="View Suspended fiber tracig activity" Command="{Binding ViewSuspendedCommand}"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>